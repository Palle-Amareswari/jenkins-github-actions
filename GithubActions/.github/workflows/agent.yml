name: Docker agent workflow

on: 
  workflow_dispatch:
  # push:
  #   branches: ['master']

jobs:
  build:
    runs-on: [ubuntu-latest]

    container:
      image: node:14

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test
